'use strict'

module.exports = {
  elements: {
    panel: {
      selector: '.panel.processes'
    },
    panelTitle: {
      selector: '.panel.processes .panel-title'
    },
    processesTable: {
      selector: '.panel.processes table'
    },
    userColumn: {
      selector: '.panel.processes td.user'
    },
    groupColumn: {
      selector: '.panel.processes td.group'
    },
    uptimeColumn: {
      selector: '.panel.processes td.uptime'
    },
    memoryColumn: {
      selector: '.panel.processes td.memory'
    },
    cpuColumn: {
      selector: '.panel.processes td.cpu'
    }
  }
}
