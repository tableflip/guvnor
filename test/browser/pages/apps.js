'use strict'

module.exports = {
  elements: {
    panel: {
      selector: '.panel.apps'
    },
    panelTitle: {
      selector: '.panel.apps .panel-title'
    },
    appsTable: {
      selector: '.panel.apps table'
    }
  }
}
