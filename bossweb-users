; # User configuration
;
; Users should follow this format:
;
; [username]
;   password = a password
;
; Then set up one or more per-host mappings for keys/secrets (where 'hostname'
; is a hostname previously declared in bossweb-hostsrc)
;
; [username.hostname]
;   key = user key
;   secret = longish string
